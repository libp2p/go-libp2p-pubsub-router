syntax = "proto3";

package namesys.pb;

message RequestLatest {
	string identifier = 1;
}

message RespondLatest {
	StatusCode status = 1;
	enum StatusCode {
		SUCCESS = 0;
		NOT_FOUND = 1;
	}
	bytes data = 2;
}